import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("screen");

export const COLORS = {
    background: '#161A30',
    title: '#072F4A',
    font: '#fff'
}

export const SIZES = {
    h1: 22,
    h2: 20,
    h3: 18,
    h4: 16,
    h5: 14,

    width,
    height,
}