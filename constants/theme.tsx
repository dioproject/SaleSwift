import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("screen");

export const COLORS = {
    background: '#F1EAFF',
    title: '#072F4A',
}

export const SIZES = {
    width,
    height,
}